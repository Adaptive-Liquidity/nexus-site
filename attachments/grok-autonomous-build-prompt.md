# Grok Autonomous Nexus-IQ Cinematic Integration Master Prompt

Attach these files to the same Grok conversation before sending the prompt below:

- `START-HERE.md`
- `README.md`
- this file, `grok-autonomous-build-prompt.md`
- `cinematic-website-tutorial-system.md`
- `sota-creative-production-extensions.md`
- `sota-marketing-research-proof-system.md`
- `cinematic-website-prompt-pack.md`
- `cinematic-website-build-checklist.md`

Then paste everything inside the following code block as the user prompt.

```text
Build the Nexus-IQ cinematic website integration to full verified completion in the current /workspace repository.

You have the attached implementation package:
- START-HERE.md — operator entrypoint, read order, authorization switches, and expected evidence.
- README.md — Nexus-IQ-specific repository context, system rules, creative direction, scope, and completion contract.
- grok-autonomous-build-prompt.md — this autonomous execution contract.
- cinematic-website-tutorial-system.md — source-faithful tutorial workflow and demonstrated implementation techniques.
- sota-creative-production-extensions.md — optional current production technologies and their adoption/security gates.
- sota-marketing-research-proof-system.md — institutional research, claim-to-proof, progressive-disclosure, ethical-curiosity, distribution, measurement, and governance rules.
- cinematic-website-prompt-pack.md — reusable asset, frontend, motion, technology-decision, research, proof, ethical-demand, distribution, and QA prompts.
- cinematic-website-build-checklist.md — compact Nexus-safe execution checklist.

OPERATING MODE
Work autonomously from inspection through verified completion. Do not stop after producing a plan, mockup, hero experiment, or partial implementation. Do not ask me routine preference questions. Make strong, reversible decisions from the live repository, the attached brief, Nexus-IQ’s existing design system, and the acceptance gates below.

Continue until every required gate passes or a genuine hard blocker remains that cannot be solved with the tools, code, assets, dependencies, or fallbacks available in the workspace. A hard task, a failed first attempt, an imperfect generated asset, a test failure, or the need to refactor is not a blocker. Diagnose, correct, and continue.

Do not deploy, publish, push, merge, create a pull request, alter external services, or change credentials unless I separately authorize that external action. Full completion means a production-ready, locally verified workspace and live preview, not an unauthorized remote release.

INSTRUCTION PRECEDENCE
1. Read and obey `/workspace/AGENTS.md` completely.
2. Read `/workspace/AGENTS.project.md` and `/workspace/.grok/project_memory.md`.
3. Read `START-HERE.md`, the attached README, and this autonomous contract completely.
4. Read the source-faithful tutorial, production extensions, marketing/research/proof system, prompt pack, and checklist completely.
5. Inspect the live repository and current branch. The populated live code and claims registry win over stale assumptions in the attachments and over generic blank-scaffold examples in repository instructions.
6. Treat this prompt as the autonomous execution and completion contract.

Do not edit `AGENTS.md` to weaken its rules. Preserve `/workspace/startup.sh`, the live-preview contract, Vercel build behavior, and all existing environment requirements.

PRIMARY OUTCOME
Transform Nexus-IQ’s website into a cohesive, premium cinematic product narrative that makes this sequence visibly understandable:

Intent → Gap → Execute → Model → Evidence → Compose → Outcomes → Trust → Evaluate

The visitor should feel and understand that a consequential autonomous action approaches a controlled boundary, is staged and constrained, is validated, commits or aborts, and emits inspectable evidence. Earn qualified curiosity through a precise unanswered mechanism and then close the loop with proof; do not use vague hype or manufactured urgency.

The experience must still look like Nexus-IQ’s forensic systems modernism. It must not look like the tutorial’s fictional Lithos/geology site, a purple luxury template, a generic AI startup, a cyberpunk dashboard, or a visual-effects demo disconnected from product truth.

NON-NEGOTIABLE PRODUCT TRUTH
- Product: Nexus-IQ.
- Category: proof-carrying transactional execution infrastructure for autonomous systems.
- Preserve the locked hero premise: “Consequential agent action belongs behind a commit boundary.”
- Preserve the existing transaction-beat sequence.
- Preserve the current DemoPlayer as interactive product proof. Never replace it with prerecorded video.
- Treat `src/content/site-copy.ts` as locked public copy unless a change is strictly required to fix a factual inconsistency and is supported by repository evidence.
- Treat `src/content/claims-registry.json` as the source of truth for maturity and limitations.
- Never remove qualifiers, maturity labels, limitations, evidence links, or Stage 0 language merely to simplify the page.
- Never promote TARGET, IN_DEVELOPMENT, EXPERIMENTAL, or LIMITATION material into an unqualified current capability.
- Generated or procedural visuals are communication assets, not proof of implementation.
- Do not fabricate benchmarks, integrations, customers, audit results, production signing, trust anchors, deployment status, or guarantees.

CONTENT, RESEARCH, PROOF, AND DEMAND
- Use Nexus-IQ's direct, compressed, mechanism-led voice. Preserve locked copy unless an evidence-supported correction is separately authorized or strictly necessary for factual consistency.
- Preserve this message spine: category tension → consequence → mechanism → proof → maturity → evaluator action.
- Map each material claim to an evidence object or label it visibly as hypothesis, target, experimental, in development, or proposed.
- For any numerical claim, expose method, unit, denominator, comparator, test conditions, uncertainty, and date.
- Build progressive disclosure for executive, technical, and evaluator depths without changing the underlying truth.
- Curiosity must expose a precise consequential gap, give an immediate partial mechanism, point to an inspectable proof object, and resolve on the same page or a clearly named destination.
- Use earned urgency only when grounded in real risk, change, availability, or cost of inaction. Never add fake countdowns, false scarcity, fabricated activity, invented customers/testimonials/logos, hidden limitations, or other dark patterns.
- Preserve contradictory, negative, and null evidence. Label systematic, scoping, and narrative reviews correctly; do not borrow PRISMA authority for a non-systematic review.
- Generated media is atmospheric communication, never research, benchmark, security, or product evidence.
- Do not add analytics, experiments, structured-data claims, outreach, paid promotion, publishing, or external research actions without separate authorization.
TECHNICAL BASELINE
Preserve the installed application and its conventions:
- React 19
- TypeScript
- Vite 8
- TanStack Start and TanStack Router
- Tailwind CSS 4
- existing Radix/shadcn-style components
- existing lucide-react icons
- existing design tokens and content architecture

Do not scaffold a second app. Do not replace TanStack routing. Do not downgrade the stack to match the tutorial. Do not add authentication, database schema, migrations, or backend features for this visual integration. If generic AGENTS.md scaffold language conflicts with the populated application, preserve the populated application and its existing routes/components.

SKILL SETUP
Before implementation, inspect `/workspace/.grok/skills` and load the applicable skill instructions directly from the workspace.

Required:
1. Read `.grok/skills/design-ui/SKILL.md` completely.
2. Read its relevant references for typography, surfaces, animations, and performance before changing those areas.

Conditional:
3. If generating or editing any image or video asset, read `.grok/skills/imagine/SKILL.md` completely before using a generation tool. Follow its generate → inspect → wire → screenshot → inspect loop.
4. If and only if the approved implementation genuinely requires Three.js/WebGL, read `.grok/skills/threejs/SKILL.md` and the relevant official reference before installing or coding it.
5. Do not load or apply auth, neon, game, multiplayer, controls, or sprite skills unless the live task unexpectedly and genuinely enters that domain. This website integration should not need them.

If a named skill is missing in the live workspace, do not invent its contents or stop. Use the repository’s existing patterns and document the missing skill in the final report.

AGENT AND ROLE SETUP
First determine whether the current Grok environment exposes subagents or parallel agent execution.

If subagents are available, use them only after the lead integrator establishes the shared architecture, tokens, asset contract, route scope, and file ownership. Assign bounded, non-overlapping responsibilities:

- Lead Integrator — owns the plan, architecture, shared contracts, integration, conflict resolution, and final verification. This is the only role allowed to change shared contracts after work begins.
- Audit and Claims Reviewer — read-only inspection of current UI, routes, claims, maturity language, and regression risks. Produces findings; does not edit implementation files.
- Research and Proof Reviewer — read-only claim-to-evidence, method, provenance, limitations, curiosity-loop, and dark-pattern audit. Produces findings; does not rewrite locked copy or publish externally.
- Cinematic/Media Worker — owns approved media assets, poster frames, media metadata, compression, and any focused video/visual component files assigned by the lead.
- UI/Motion Worker — owns the explicitly assigned homepage components/hooks for Intent → Gap → Execute. Must reuse tokens and must not edit claims or locked copy.
- QA/Accessibility Reviewer — read-only browser, interaction, responsive, reduced-motion, performance, and claim-safety verification after integration.

Before parallel work, record exact ownership in the plan. Do not let multiple workers edit `src/routes/index.tsx`, `src/styles.css`, token files, or the same shared component concurrently. The lead integrator performs shared-file edits and final integration.

If subagents are not available, execute the same roles sequentially yourself. Do not block or reduce scope merely because parallel agents are unavailable.

TECHNOLOGY AND DEPENDENCY DECISION GATE
1. Inspect `package.json`, lockfile, existing imports, and browser support before installing anything.
2. Classify the approved surface as exactly one primary path: DOM/CSS, native scroll/view transition, video, shader, or interactive 3D.
3. Prefer existing React/Tailwind/utilities plus native CSS transitions/keyframes, Web Animations API, CSS Scroll-Driven Animations, View Transition API, `IntersectionObserver`, `requestAnimationFrame`, SVG/canvas, and HTML video.
4. Use GSAP + ScrollTrigger only for a complex synchronized pinned timeline that would be materially more brittle or larger with native code. Use a React-safe lifecycle, clean up every trigger/context, test reverse scroll and responsive refresh, and document why native code was insufficient.
5. Use Motion only for a genuine React layout/state choreography requirement. Do not add Motion and GSAP for overlapping behavior.
6. Use native scrolling by default. Add Lenis only after native scrolling fails the approved feel and after testing anchors, keyboard, wheel, touch, sticky/pinned behavior, history restoration, reduced motion, and teardown.
7. For a shader-only scene that does not need a scene graph, consider OGL before Three/R3F.
8. Use Three.js + React Three Fiber v9, with only necessary Drei helpers, only for true camera/lighting/spatial/interactivity requirements that DOM, SVG, canvas, or media cannot meet. Lazy-load it, cap DPR, use demand rendering where possible, pause hidden/offscreen work, dispose resources, measure bundle/GPU cost, and provide static/no-WebGL/context-loss/reduced-motion fallbacks.
9. Do not install competing rendering or motion stacks “just in case.” Do not add ReactBits, Packery, tunnel-rat, Rapier, Matter.js, Tokens Studio, Style Dictionary, Storybook, Leva, or Lygia unless the implementation map names the exact requirement and acceptance test.
10. For every new dependency, record: visual requirement, alternatives considered, bundle/runtime cost, accessibility/browser fallback, removal condition, and why existing tools were insufficient.
11. Never create `.env` files or hardcode secrets.
12. Run a dependency/security audit after dependency changes and distinguish pre-existing findings from introduced findings.

DEFAULT ASSET STRATEGY
Choose the simplest strategy that can produce the approved visual quality:

A. Evolve the existing `HeroSchematic` and current DOM/CSS system.
B. Build a precise procedural visual with semantic HTML/SVG/canvas when exact states or labels matter.
C. Generate one verified cinematic still and, only if an available video tool can do so reliably, one restrained motion asset.
D. Use Three.js only when A–C cannot meet the agreed creative direction.

Never generate technical diagrams, labels, evidence fields, numbers, or product screenshots as pixels. Build exact information in code. Generated media may supply atmosphere and abstract visual form only.

If video generation is unavailable, continue with a complete high-quality solution using procedural/code motion and static generated assets. Lack of a video tool is not a blocker.

EXTERNAL ASSET AND DESKTOP TOOL BOUNDARY
- Blender MCP is a separate trusted desktop workflow, not a Grok Build dependency. Do not install or control Blender in this sandbox.
- FLUX, Kling, Luma, Cloudinary, and Mux are optional credentialed services. Without separate authorization, do not create accounts, spend credits, upload assets, configure remote MCPs, change credentials, or mutate external resources.
- You may prepare an asset brief, prompt, manifest schema, or integration adapter that does not require credentials.
- When an external action is authorized, obey its named service, asset purpose, spend limit, data boundary, and rollback scope exactly.
- Never send repository source, claims data, credentials, private customer data, or unrelated assets to an external generation or media service.
- Model names, API endpoints, prices, and terms are time-sensitive; verify current official documentation immediately before authorized use.

MEDIA CONTRACT
- Store approved media under `public/media/cinematic/` with descriptive names.
- Include a poster/still fallback for every video.
- Inspect every asset before wiring it into the app.
- Preserve source/licensing notes in `docs/build/CINEMATIC_ASSET_MANIFEST.md`, including origin, tool/model/version, prompt/settings, rights status, original/final sizes, transformations, fallback, crop notes, and external IDs when applicable.
- Never ship a Pinterest/reference image as a production asset.
- Do not include fake text, logos, UI, source code, signatures, or evidence fields inside generated media.
- Background video must be muted and `playsInline`.
- Loop only when the clip is visually seamless and looping is actually useful.
- A scroll-scrubbed video must not autoplay against `currentTime` control.
- Use responsive crop/object-position and verify the focal point at every target viewport.
- Prefer a target of no more than 8 MB for an initial hero video and 5 MB for later lazy-loaded motion assets. If those targets cannot be met without visible damage, document the tradeoff and prevent unnecessary mobile/initial loading.
- Respect reduced-data and reduced-motion conditions with static fallbacks when practical.
- Critical product meaning must remain available without media playback.
- If GLB/glTF is used, validate and optimize it with glTF Transform or an equivalent measured pipeline. Remove unused data, resize/compress textures, evaluate KTX2/Basis and Meshopt/Draco, preserve required node/clip names, record before/after sizes, and provide a poster/static fallback.

CREATIVE DIRECTION
Use the existing Nexus-IQ tokens and visual identity:
- dark runtime surfaces: void, carbon, slate, slate-elevated;
- evidence/archive surfaces: archive, archive-muted, archive-ink;
- copy: porcelain families;
- institutional accent: institution;
- maturity/status colors only for their defined semantic states;
- IBM Plex Serif/Sans/Mono token stack.

Do not transplant the tutorial’s `#522DFF`, Playfair styling, planet imagery, geology copy, pricing, fictional routes, or “$50,000” framing.

The central visual should communicate a consequential action approaching a controlled commit boundary. Favor:
- one focal object or state transition;
- intentional negative space;
- staged and constrained movement;
- deterministic validation;
- distinct commit and abort outcomes;
- an evidence trace emitted after decision.

Avoid generic AI brains, humanoid robots, circuit-board filler, crypto motifs, neon-purple gradients, random particles, excessive glass panels, glowing borders, and repetitive cards.

AUTONOMOUS EXECUTION LEDGER
Create and maintain:

`/workspace/docs/build/CINEMATIC_INTEGRATION_PLAN.md`

It must contain:
- baseline commit/branch and inspection timestamp;
- current-state findings;
- exact implementation scope;
- chosen visual/asset strategy and rejected alternatives;
- skill files loaded;
- new dependency decisions;
- message spine and claim-to-evidence findings for the focused sequence;
- unresolved evidence, method, maturity, limitation, or ethical-demand risks;
- agent/role ownership if used;
- phase checklist with status: pending, in progress, passed, failed, blocked;
- verification evidence and screenshot paths;
- open defects and their resolution;
- final completion decision.

Update the ledger at every phase boundary. This is an execution record, not a substitute for implementation. Do not stop after writing it.

PHASE 0 — PREFLIGHT AND RECOVERY SAFETY
1. Read all governing files and attachments.
2. Determine branch, status, existing changes, and current project state.
3. Preserve user/existing changes. Do not reset, overwrite, or delete unrelated work.
4. Inspect `package.json`, every build script, `scripts/migrate.mjs`, `startup.sh`, Vite config, TypeScript config, routes, design tokens, claims registry, project memory, public assets, and current screenshots/build notes. Determine whether any validation command can mutate an external database or service. Check only whether a database target is external/credentialed; never print secret values.
5. Ensure `startup.sh` remains idempotent and matches the actual start command.
6. Start or recover the dev server through the existing contract.
7. Confirm the homepage responds before changing code.
8. Create the execution ledger.

Phase 0 exit gate:
- governing instructions read;
- workspace state understood;
- dev preview healthy or exact pre-existing blocker identified;
- no unrelated work destroyed;
- ledger created;
- build/migration mutation risk classified and a safe local build command selected.

PHASE 1 — BASELINE PRODUCT AND VISUAL AUDIT
1. Open the live site with browser/Playwright tooling.
2. Capture baseline screenshots under `/workspace/screenshots/cinematic-baseline/` at approximately:
   - 390 × 844 mobile;
   - 768 × 1024 tablet;
   - 1440 × 900 desktop.
3. Inspect and exercise:
   - hero and navigation;
   - Intent → Gap → Execute transition;
   - DemoPlayer controls and commit/abort paths;
   - transaction rail/connectors;
   - evidence and maturity surfaces;
   - footer/evaluation CTA;
   - every public route at least for load and obvious layout failure.
4. Inspect browser console and failed network requests.
5. Record what is strong, what is flat/repetitive, what motion could clarify, and where motion would harm comprehension.
6. Audit the focused sequence for message spine, claim/evidence links, maturity, methods, limitations, unresolved curiosity loops, fake urgency, and dark patterns.
7. Identify exact components/files to change and surfaces explicitly left untouched.

Phase 1 exit gate:
- screenshots inspected, not merely generated;
- interaction baseline recorded;
- claim/maturity/evidence baseline recorded;
- content, curiosity, and ethical-demand findings recorded;
- implementation map names exact files;
- no code changes made before the audit is complete.

PHASE 2 — SHARED DESIGN, MOTION, AND ASSET CONTRACT
1. Define the cinematic narrative for Stage → Constrain → Validate → Commit/Abort → Emit.
2. Map it onto the existing Intent → Gap → Execute structure.
3. Define the focused message spine, exact open question, resolution point, Proof Capsule, maturity/limitations treatment, and evaluator next action.
4. Classify the surface and choose one primary runtime path using the technology decision gate; record rejected alternatives.
5. Decide the simplest viable asset strategy using the A–D hierarchy above.
6. Define:
   - focal object;
   - negative-space location;
   - desktop/mobile composition;
   - scroll range and state mapping;
   - reduced-motion fallback;
   - color handoff into existing surfaces;
   - poster/loading behavior;
   - performance budget;
   - component and hook boundaries.
7. Reuse existing tokens. Add new semantic tokens only when genuinely reusable.
8. Define the dependency removal condition and all native/static fallbacks.
9. Establish agent file ownership before any parallel implementation.
10. Record the contract in the ledger.

Phase 2 exit gate:
- one coherent direction selected;
- architecture and ownership are unambiguous;
- no claims depend on generated visuals;
- the focused curiosity loop resolves into an inspectable mechanism or evidence object;
- all material claims retain maturity, method, provenance, and limitations;
- asset and performance plans have fallbacks.

PHASE 3 — ASSET/PROCEDURAL PROTOTYPE
1. Build the smallest representative visual prototype.
2. If using an authorized or workspace-provided generation tool, load the imagine skill, verify the allowed service/data/spend boundary, create one base still, inspect it, and allow at most one targeted regeneration before simplifying the direction. If no authorized tool exists, use the procedural/static fallback without pausing.
3. If using code, ensure exact states/geometry are semantic and controllable.
4. Test the prototype in the real hero slot at mobile and desktop sizes.
5. Verify negative space, focal crop, color compatibility, and first-frame behavior.
6. Reject the prototype if it looks generic, conflicts with the current system, weakens readability, or implies unsupported capabilities.

Phase 3 exit gate:
- prototype has been visually inspected in the app;
- approved direction works at mobile and desktop;
- fallback state works;
- no broken asset requests;
- dependency choice remains justified.

PHASE 4 — INTENT HERO IMPLEMENTATION
1. Integrate the approved visual into the existing Intent hero.
2. Preserve locked headline, supporting copy, CTAs, maturity strip, transaction chrome, and semantic heading structure.
3. Evolve rather than discard `HeroSchematic` unless the audit proves replacement is necessary.
4. Maintain clear hierarchy and negative space.
5. Do not rely on blanket dark overlays. Fix composition, crop, placement, text color, or asset content first.
6. Keep controls/links keyboard accessible and target sizes usable.
7. Add loading, poster, error, and reduced-motion states.
8. Verify after integration at all target viewports.

Phase 4 exit gate:
- hero value is more legible than baseline;
- copy is readable across every media state;
- CTAs/navigation/maturity remain intact;
- no mobile overflow;
- reduced-motion fallback is complete.

PHASE 5 — GAP PINNED/SCROLL-LINKED NARRATIVE
1. Carry the visual premise through the Gap beat only as long as it clarifies the missing control boundary.
2. If using scroll-linked playback/state:
   - wait for metadata when using video;
   - map a clamped 0–1 scroll range to deterministic state/time;
   - schedule updates with requestAnimationFrame;
   - use passive listeners where appropriate;
   - support forward and reverse scrolling;
   - clean up listeners, observers, and pending frames;
   - prevent normal autoplay from fighting scrubbing;
   - avoid React state updates on every scroll frame when imperative refs suffice.
3. Use `IntersectionObserver` for discrete reveals, not continuous scroll polling.
4. Ensure pinned/sticky behavior releases at the correct boundary.
5. Keep the transaction rail and text sequence understandable.
6. Provide a static/simplified reduced-motion experience with all content visible.

Phase 5 exit gate:
- forward and reverse motion are smooth and deterministic;
- sticky state never traps or overlaps later sections;
- no scroll jank or listener leaks;
- mobile layout remains usable;
- reduced-motion content is complete.

PHASE 6 — EXECUTE/DEMOPLAYER HANDOFF
1. End the cinematic sequence before it competes with the DemoPlayer.
2. Transition into the existing surface using a transparent-to-tokenized background handoff without a hard seam.
3. Isolate video/blend/blur layers so compositing cannot affect headings or controls.
4. Preserve DemoPlayer functionality, scenario selection, scrub behavior, commit/abort comparison, evidence inspection, and maturity context.
5. Verify that the DemoPlayer remains the strongest proof surface on the page.

Phase 6 exit gate:
- handoff is visually seamless;
- DemoPlayer is fully interactive;
- cinematic media does not masquerade as the live demo;
- no compositing artifacts or contrast failures.

PHASE 7 — SITE-WIDE HARMONIZATION
Continue automatically after the first sequence passes, but restrict this phase to non-structural regression repair and shared-system polish. Do not redesign routes, create new routes, add route-specific cinematic assets, add a second media pipeline, or broaden the narrative scope without separate user authorization.

1. Inspect Model, Evidence, Compose, Outcomes, Trust, Evaluate, footer, and public routes for regressions caused by the upgraded first sequence.
2. Harmonize only what is necessary for one coherent site:
   - spacing rhythm;
   - redundant wrappers;
   - excessive vertical gaps;
   - inconsistent borders/strokes;
   - motion timing;
   - token use;
   - media-to-solid transitions.
3. Preserve the dark-runtime versus archive-evidence semantic distinction.
4. Do not place motion in every section.
5. Do not add a later cinematic bridge in the default run. Record it as an optional next milestone if it would materially improve closure.
6. Do not add or redesign route-specific art in the default run. Existing routes must load and remain at least baseline-quality.
7. Do not change locked copy or claim maturity for visual convenience.

Phase 7 exit gate:
- the focused sequence is coherent and no untouched section/route regressed below baseline;
- no section is visually orphaned;
- no motion overload;
- density improved where needed without deleting evidence or meaning.

PHASE 8 — ACCESSIBILITY, RESPONSIVE, AND PERFORMANCE HARDENING
1. Test at 390 × 844, 768 × 1024, and 1440 × 900; add a wide desktop check if needed.
2. Test keyboard navigation, visible focus, landmarks, heading order, buttons, links, DemoPlayer controls, and any accordion/dialog behavior.
3. Test `prefers-reduced-motion: reduce` and confirm all information remains visible.
4. Check text contrast across every frame/state of media.
5. Check zoom at 200% and no essential clipping/horizontal overflow.
6. Inspect console errors, unhandled promise rejections, hydration warnings, and failed network requests.
7. Measure media byte sizes and initial requests.
8. Lazy-load noncritical visuals and routes where appropriate.
9. Avoid layout shifts by reserving dimensions and using poster/fallback states.
10. If CSS Scroll-Driven Animations or View Transitions are used, test feature detection and the non-supporting fallback.
11. If WebGL is used, test fallback, cap DPR, hidden/offscreen pausing, disposal, and context loss/re-entry where practical.
12. Check mobile power/data behavior; avoid eagerly loading nonessential background videos.

Phase 8 exit gate:
- accessibility checks pass;
- target viewports pass;
- no console/network failures;
- media and bundle costs are measured and acceptable or explicitly mitigated;
- reduced motion is a complete experience.

PHASE 9 — CLAIM, REGRESSION, AND BUILD VERIFICATION
1. Compare public copy and maturity language against `site-copy.ts` and `claims-registry.json`.
2. Confirm no limitation, status, or evidence link was lost.
3. Load every public route and verify navigation.
4. Re-run critical interactions, including DemoPlayer commit/abort paths.
5. Run validation with an explicit external-mutation safety gate:
   - `npm run typecheck`
   - `npm run lint`
   - inspect the current `build` and migration scripts before executing them;
   - the current `npm run build` chains `db:migrate`; run it only when the migration target is confirmed local and non-destructive;
   - if `DATABASE_URL` is external, credentialed, or uncertain, do not print it and do not run the migration; run `npm exec -- vite build` as the compile-only build verification and record the skipped migration as not authorized, not passed
6. If a command fails:
   - determine whether it is introduced or pre-existing;
   - fix introduced failures;
   - fix relevant pre-existing failures when safely in scope;
   - rerun the exact command;
   - do not report a pass until the command exits successfully.
7. Run an appropriate dependency/security audit if dependencies changed. Never hide findings; separate introduced from pre-existing risk.
8. Keep the live preview healthy after build verification.

Phase 9 exit gate:
- typecheck passes;
- lint passes, or a precisely identified pre-existing unrelated lint baseline is documented with no new violations;
- the safe local build gate passes: full `npm run build` only when its migration is confirmed local/safe, otherwise compile-only `npm exec -- vite build` passes and the migration is explicitly reported as skipped/not authorized;
- public routes and critical interactions pass;
- claims audit passes;
- live preview remains healthy.

PHASE 10 — FINAL VISUAL QA AND COMPLETION REPORT
1. Capture final screenshots under `/workspace/screenshots/cinematic-final/` at the same baseline viewports and important scroll states.
2. Inspect them side-by-side with baseline screenshots.
3. Correct clipping, weak hierarchy, excessive emptiness, generic effects, inconsistent spacing, bad cropping, contrast loss, or unearned decoration.
4. Perform one final browser walkthrough from hero through Evaluate and across public routes.
5. Update `/workspace/docs/build/CINEMATIC_INTEGRATION_PLAN.md` so every phase has a truthful final state.
6. Create `/workspace/docs/build/CINEMATIC_INTEGRATION_REPORT.md` with:
   - final outcome;
   - before/after explanation;
   - architecture and visual decisions;
   - skill files used;
   - agent roles used;
   - dependencies added/removed and reasons;
   - exact files/assets changed;
   - media dimensions, durations, formats, and byte sizes;
   - verification command results, including the exact safe build command and any intentionally skipped migration;
   - browser/viewports/interactions tested;
   - screenshots inspected;
   - claim-safety confirmation;
   - deferred items and known limitations.
7. Update `.grok/project_memory.md` with a concise, factual record of the completed integration and verified commands. Do not record aspirational claims as completed.
8. Leave the dev server running through the established startup contract.

FINAL COMPLETION GATES
Do not call the task complete unless all applicable items are true:
- The current site was inspected before editing.
- All eight attachments and required skills were read.
- The chosen visual is Nexus-IQ-specific and not a tutorial clone.
- Intent → Gap → Execute forms one coherent cinematic/product sequence.
- DemoPlayer remains interactive and dominant as proof.
- Untouched homepage sections and public routes load without regression; redesigning them is not required for this focused milestone.
- Locked copy, claims, maturity labels, limitations, methods, provenance, and evidence survive.
- Changed curiosity loops resolve into mechanism or evidence; no fake scarcity, fabricated proof/social proof, or dark pattern was introduced.
- Desktop, tablet, and mobile have been visually inspected.
- Forward and reverse scroll behavior pass.
- Reduced-motion behavior passes.
- Keyboard/focus/contrast checks pass.
- No console errors, missing assets, or hydration failures remain.
- Typecheck passes.
- The safe local build gate passes; no external migration was executed without authorization.
- Lint has no introduced violations.
- Media/bundle costs are measured and mitigated.
- Plan ledger and final report are complete.
- Live preview is healthy and left running.

RECOVERY AND PERSISTENCE RULES
- Never solve a failure by deleting unrelated user work, resetting the repository, weakening tests, hiding errors, removing claim qualifiers, or disabling accessibility behavior.
- When an asset fails, simplify or switch strategy: generated video → generated still + code motion → pure code/procedural visual → evolved existing schematic.
- When a dependency causes build/bundle problems, remove it and use the simpler strategy.
- When scroll behavior is unstable, reduce the mapping complexity before abandoning the narrative.
- When text contrast fails, fix composition/crop/placement/asset; reject the placement if it still fails.
- When a browser-specific compositing issue appears, isolate layers and provide a simpler fallback.
- After every fix, rerun the failing check and the nearest regression checks.
- Keep a maximum of one active implementation direction. Do not accumulate abandoned components/assets/dependencies.
- Clean up unused experimental files and imports after the chosen direction is complete, while preserving unrelated existing work.

BLOCKING RULE
Do not ask me for routine decisions, visual approval, command execution, screenshots, or debugging help. Choose the strongest option supported by the brief and verify it yourself.

Stop and report a blocker only if completion requires authority or information that cannot be safely inferred or replaced, such as:
- credentials, an external service change, or permission to run an external database migration;
- legal approval for a specific third-party asset when no generated/procedural fallback can satisfy the design;
- permission to deploy, publish, push, merge, or modify external resources;
- a materially changed product truth that conflicts with the claims registry and requires owner resolution;
- an unavailable platform capability with no viable code or asset fallback.

Before declaring a blocker, exhaust safe in-scope fallbacks and record what was tried.

FINAL USER RESPONSE
When all gates pass, give me a concise product-level summary followed by:
1. what changed;
2. the main creative and technical decisions;
3. exact verification results, including the build command used and migration status;
4. screenshots inspected;
5. dependencies/assets introduced, including the selected runtime classification and rejected alternatives;
6. known limitations or deliberately deferred work;
7. confirmation that the live preview is running.

Do not ask “does this look good?” as a substitute for your own QA. Do not end with an unfinished plan. Build, inspect, correct, verify, and finish.
```

